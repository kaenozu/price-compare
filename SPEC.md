# Price Compare - 計算仕様書 (Phase 1)

## 概要
スーパードラッグストア・ネット通販の商品価格を比較するための計算エンジン仕様。

## 計算順序

```
税抜価格
  → 税計算（8%/10%）
  → 商品値引き（割引率 or 固定額）
  → クーポン適用
  → 使用ポイント充当
  → 送料加算
  → 獲得ポイント評価
```

**注意:** 実店舗や通販サイトによって適用順序が異なる場合がある。MVPでは上記順序に固定。

## 代表値

| 項目 | 説明 |
|------|------|
| `payableNow` | 今回実際に支払う金額 |
| `effectiveCost` | `payableNow - 獲得ポイントの評価額` |
| `unitCost` | `effectiveCost / 正規化された容量` |

## 丸めルール

- すべての金額計算: `HALF_UP`
- 単位価格: 小数点以下6桁 `HALF_UP`
- 勝敗判定: 交差積を使用（除算の丸めによる誤判定を防止）

## 未入力の扱い

- `null` = 未入力（値が不明）
- `0` = ゼロ確認済み（値は0であると確認済み）
- 未入力の場合、依存する計算以降を `null` にし、警告を返す
- 例: 送料未入力 → `payableNow` 以降を `null` に

## 税計算

- 税率: `Offer` ごとに明示（8%, 10%, 0%）
- 税込価格 = 税抜価格 × (1 + 税率)
- 端数処理: 各商品段階で `HALF_UP`（単一オファー比較見積り前提）

## ポイント評価

- デフォルト: 1ポイント = 1円
- ユーザー設定で変更可能（0.0〜2.0）
- 獲得ポイント価値が支払額を超える場合: 負値を許容、警告付き

## 単位正規化

| 次元 | 基準単位 | 変換先 |
|------|----------|--------|
| 重量 | g | kg → g (×1000) |
| 容量 | ml | L → ml (×1000) |
| 個数 | count | 個/枚/本/袋 → count |

**異次元比較は不可:** 500ml と 600g → 比較不能

## 比較結果 (ComparisonResult)

```
商品A:
  支払額: 1,180円
  実質負担額: 1,080円
  100g当たり: 216円

商品B:
  支払額: 1,240円
  実質負担額: 1,040円
  100g当たり: 208円

結果:
  支払額ではAが60円安い
  ポイント込みではBが40円安い
  単位価格ではBが約3.7%安い
```

## 送料

- 送料は商品単位ではなく概念的に分離（注文全体にかかる場合がある）
- 送料無料条件: クーポン適用後・ポイント使用前の金額で判定

## 割引

- 割引はリストとして保持（適用順序固定）
- 10%引き後に100円引き ≠ 100円引き後に10%引き

## 制限事項 (Phase 1)

- 単一オファーの比較見積り（複数明細レシートの完全再現は対象外）
- バーコード商品データベース: 対象外
- レシートOCR: 対象外
- ネット通販価格自動取得: 対象外
