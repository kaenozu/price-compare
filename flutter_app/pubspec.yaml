name: price_compare_flutter
description: Minimal Flutter UI and Dart calculation engine for price comparison.
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
